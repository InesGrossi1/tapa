document.addEventListener("DOMContentLoaded",()=>{const form=document.querySelector("[data-home-search]");if(form){form.addEventListener("submit",e=>{e.preventDefault();const q=form.querySelector("input").value.trim();if(q)location.href="/search/?q="+encodeURIComponent(q)})}const box=document.querySelector("[data-search-input]");const results=document.querySelector("[data-search-results]");if(box&&results){fetch("/search-index.json").then(r=>r.json()).then(data=>{const render=()=>{const q=box.value.toLowerCase().trim();if(!q){results.innerHTML="<p class='pagination-note'>Type a Windows, app, driver, network, or PC problem.</p>";return}const words=q.split(/\s+/);const found=data.filter(x=>words.every(w=>(x.title+" "+x.keyword+" "+x.category).toLowerCase().includes(w))).slice(0,40);results.innerHTML=found.length?found.map(x=>`<a class="search-result" href="${x.url}"><strong>${x.title}</strong><br><small>${x.category} · ${x.keyword}</small></a>`).join(""):"<p>No matching guide found.</p>"};box.addEventListener("input",render);const params=new URLSearchParams(location.search);if(params.get("q")){box.value=params.get("q");render()}})}});